#include "holberton.h"

/**
 * main - code
 *
 * Return: Always 0.
 */
int main(__attribute__((unused))int argc, char *argv[])
{
/*	int i;*/
	char *a = argv[0];

	_putchar(*(a +1));

/**
	for (i = 0; argv[0][i] != '\0'; i++)
	{
		_putchar("argv[0][i]");
	}
*/

return (0);
}
